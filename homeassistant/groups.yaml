default_view:
  view: yes
  icon: mdi:home
  entities:
    - group.ldr
    - group.climate
    - group.dht
    - group.reed_switch
ldr:
  name: Light
  view: no
  entities:
    - sensor.ldr
dht:
  name: DHT
  view: no
  control: hidden
  entities:
    - sensor.temperature
    - sensor.humidity
reed_switch:
  name: Reed Switch
  view: no
  entities:
    - sensor.reed_switch
climate:
  name: Climate
  view: no
  entities:
    - sensor.weather_temperature
    - sensor.weather_symbol